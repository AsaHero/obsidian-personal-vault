Как быть с Горутинами которые заблокированы из за [[Synchronization Primitives#Mutex|мютекса]].  Мы точно знаем что Горутины должна подождать в какой то очереди, пока другая Горутина выйдет из опасной зоны.

На каком уровне работают мьютексы ?

Если возьмем и будем использовать мьютексы операционный системы, то при блокировке опасной зоны заблокируется сам поток. И тогда дальше некому выполнять Горутины из очередей. 

А нам нужен свой мьютекс, который на уровне приложение, который блокирует только Горутину, а поток продолжит свое выполнение.

А вот теперь более подробно о очереди ожидающих горутин. Там находятся Горутины в статусе Waiting, и как можно архитектурно это реализовать:

```c
/*
 * Simple, straightforward mutexes with strict semantics:
 *
 * - only one task can hold the mutex at a time
 * - only the owner can unlock the mutex
 * - multiple unlocks are not permitted
 * - recursive locking is not permitted
 * - a mutex object must be initialized via the API
 * - a mutex object must not be initialized via memset or copying
 * - task may not exit with mutex held
 * - memory areas where held locks reside must not be freed
 * - held mutexes must not be reinitialized
 * - mutexes may not be used in hardware or software interrupt
 *   contexts such as tasklets and timers
 */
struct mutex {
    atomic_long_t       owner;
    raw_spinlock_t       wait_lock;
#ifdef CONFIG_MUTEX_SPIN_ON_OWNER
    struct optimistic_spin_queue osq; /* Spinner MCS lock */
#endif
    struct list_head      wait_list;
#ifdef CONFIG_DEBUG_MUTEXES
    void            *magic;
#endif
#ifdef CONFIG_DEBUG_LOCK_ALLOC
    struct lockdep_map  dep_map;
#endif
};
```

Это структура `struct mutex` из заголовочного файла ядра Linux (`include/linux/mutex.h`).

Сильно глубоко не нужно понимать, тут самое главное то то что у каждого мьютекса есть своя очередь `wait_list`. Который выглядит в следующим образом:

![[Pasted image 20260916184230.png]]


Это первый вариант как можно реализовать mutex c wait queue.

И теперь след вариант, так же реализовал сам Go:

Вот код с этого фото:

```go
// A semaRoot holds a balanced tree of sudog with distinct addresses (s.elem).
// Each of those sudog may in turn point (through s.waitlink) to a list
// of other sudogs waiting on the same address.
// The operations on the inner lists of sudogs with the same address
// are all O(1). The scanning of the top-level semaRoot list is O(log n),
// where n is the number of distinct addresses with goroutines blocked
// on them that hash to the given semaRoot.
// See golang.org/issue/17953 for a program that worked badly
// before we introduced the second level of list, and
// BenchmarkSemTable/OneAddrCollision/* for a benchmark that exercises this.
type semaRoot struct {
    lock  mutex
    treap *sudog        // root of balanced tree of unique waiters.
    nwait atomic.Uint32 // Number of waiters. Read w/o the lock.
}

var semtable semTable
```

Это фрагмент из рантайма Go (`runtime/sema.go`) — реализация семафоров.

Тут нет никаких локальный очередей которые бы хранили ожидающие Горутины. Но есть глобальный пул, не важна какая структура данных хоть дерево граф или связанный список, но факт в том что туда будут складываться Горутины которые заблокированы из какого либо мьютекса, по адресу или дискриптеру мьютекса. В го как раз используется сбалансированное дерево:

![[Pasted image 20260916184910.png]]

А не будут ли Голодать Горутины в очереди, посмотрим два режима мьютексов:

![[Pasted image 20260916185024.png]]

Тут пока из очереди выйдет из ожидание попадет в LRQ/GRQ, и только потом начнет  выполняться и за это время может другая Горутина вне очрежи может прийти первым и забрать мьютекс а то который вышел из очреди попадет снова в лок и пойдет в конец очереди. 

А также еще есть starvation mode:

![[Pasted image 20260916185237.png]]

Тут перед тем как выйти из опасный зоны горутина разблокирует мьютекс и при раззаблокировании из очреди берется следующая горутина и передает мьютекс на выполнение это горутины и потом переводит горутину в статус Runnable. И так образом пока горутина на пути выполнение не потеряет доступ к мьютексу.